"""WealthOS backend package.

Package root for the modular monolith. Application wiring lives under
`app/`, cross-cutting runtime under `core/`, shared helpers under `shared/`,
and business capabilities under `modules/` (added when a domain is built).
"""

__version__ = "0.1.0"
